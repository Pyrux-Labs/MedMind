import LanguageDropdown from "../ui/LanguageDropdown";

const NavBar = () => {
    return (
        <div>
            NavBar
            <LanguageDropdown />
        </div>
    );
};

export default NavBar;
